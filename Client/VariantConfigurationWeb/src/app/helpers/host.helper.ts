export class Host {
    hostForImg = location.protocol + '//' + location.host + '/www';
    // hostForImg = '';
    host = location.protocol + '//' + location.host + '/';
    // host = 'http://localhost:50003/';
}
